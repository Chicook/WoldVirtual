# from vpython import *
# from Entorno3DBS2 import create_scene
# from Entorno3DBS3 import create_structure
# from Entorno3DBS4 import create_sphere
# from Entorno3DBS5 import animate_sphere, create_block, validate_block

# Crear la escena
# scene = create_scene()

# Crear la estructura de la estación espacial
# structure = create_structure()

# Crear la esfera animada
# sphere = create_sphere()

# Animar la esfera
# animate_sphere(sphere)

# Crear y validar bloques en la blockchain
# block1 = create_block('Entorno3DBS1', 'Genesis Block')
# block2 = create_block('Entorno3DBS2', block1['hash'])
# block3 = create_block('Entorno3DBS3', block2['hash'])
# block4 = create_block('Entorno3DBS4', block3['hash'])
# block5 = create_block('Entorno3DBS5', block4['hash'])

# blocks = [block1, block2, block3, block4, block5]

# for block in blocks:
    # validate_block(block)
